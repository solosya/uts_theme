{{ set(this, 'title', _Blog.getBlogTitle()) }}
{% set networkData = _Network.getNetworkData() %}

{{this.registerJs("HomeController.listing();")}}
{% set limit = 20 %}

{% set articlesArr = _Blog.getBlogFeed({'limit': limit, 'offset': 0}) %}
{% set articles = articlesArr.articles %}

<!-- used to index each article as we loop over throughout the template -->
{% set articleCount = 0 %}


{% set sidePanel =  "Breaking News" %}
{% set sidePanelfeed = [] %}
{% for blog in networkData.networkBlogs %}
    {% if blog.title == sidePanel %}
        {% set sidePanelfeed = _Blog.getBlogFeed({'limit': 5, 'offset': 0, 'blogid':blog.guid}) %}
    {% endif %}
{% endfor %}





    <!--placeholder temp -->
    <section class="channel container">
        <div class="row">
            <div class="col-sm-12">
                <div class="row">
                    {% for i in 0..1 %}
                        {% if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-6 card-section-feature'}) | raw}}
                            {% set articleCount = articleCount + 1 %}
                        {% endif %}
                    {% endfor %}
                </div><!--row-->
            </div>
        </div>

    </section><!--featured-carousel-->

    <section class="content-block container">
        <div class="row">
            <div class="col-sm-12">
                <div class="row">
                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-6 card-md'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-3 card-sm'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-3 card-sm'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-3 card-sm'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-3 card-sm'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                    {% if articles[articleCount] %}
                        {{this.render('partials/_single-article.twig', {article: articles[articleCount], 'position': (articleCount + 1 ), containerClass: 'col-sm-6 card-md'}) | raw}}
                        {% set articleCount = articleCount + 1 %}
                    {% endif %}

                </div><!--row-->
            </div>
        </div>
    </section>

    <div class="container">
        <h5>2SER</h5>
        <section class="content-block ser">
            <div class="row">
                <div class="col-sm-4">
                    <img src="{{networkData.templatePath}}/static/images/ser.png" alt="ALT">
                </div><!--col-sm-4-->
                <div class="col-sm-4">
                    <article class="card other">
                        <div class="content">
                            <span class="author">Sophie Mckailero  - </span>
                            <time datetime="2016-11-16">16 Sep 2016</time>
                            <h2>National Biochemistry Week Listen to the highlights</h2>
                            <p>Ribus eosanim exero omnit, asnis masioel oto a tempum esciis am aditate et vent latis volorpo renimpo resequi que praernam nred es demquis solorum sequatem incilic ipsaesc imiliasped magnate</p>
                        </div>
                    </article>
                </div><!--col-sm-4-->
                <div class="col-sm-4">
                    <article class="card other">
                        <div class="content">
                            <span class="author">Sophie Mckailero  - </span>
                            <time datetime="2016-11-16">16 Sep 2016</time>
                            <h2>National Biochemistry Week Listen to the highlights</h2>
                            <p>Ribus eosanim exero omnit, asnis masioel oto a tempum esciis am aditate et vent latis volorpo renimpo resequi que praernam nred es demquis solorum sequatem incilic ipsaesc imiliasped magnate</p>
                        </div>
                    </article>
                </div><!--col-sm-4-->
            </div><!--row-->
        </section><!--ser-->
    </div>

    <section class="highlight content-block">
        {% if articles[7] %}
            {{this.render('partials/_single-article.twig', {article: articles[7], 'position': (8), containerClass: 'card-lg', cardClass : 'other'}) | raw}}
        {% endif %}
    </section><!--highlight-->

    <section class="content-block container">
        <div class="row">

            <div class="col-sm-12">
                <div class="row">

                    {% if articles[8] %}
                        {{this.render('partials/_single-article.twig', {article: articles[8], 'position': (9), containerClass: 'col-sm-4 card-sm', cardClass : 'instagram'}) | raw}}
                    {% endif %}
                    {% if articles[9] %}
                        {{this.render('partials/_single-article.twig', {article: articles[9], 'position': (10), containerClass: 'col-sm-8 card-md', cardClass : 'youtube'}) | raw}}
                    {% endif %}
                    {% if articles[10] %}
                        {{this.render('partials/_single-article.twig', {article: articles[10], 'position': (11), containerClass: 'col-sm-4 card-sm', cardClass : 'twitter'}) | raw}}
                    {% endif %}
                    {% if articles[11] %}
                        {{this.render('partials/_single-article.twig', {article: articles[11], 'position': (12), containerClass: 'col-sm-4 card-sm', cardClass : 'soundcloud'}) | raw}}
                    {% endif %}
                    {% if articles[12] %}
                        {{this.render('partials/_single-article.twig', {article: articles[12], 'position': (13), containerClass: 'col-sm-4 card-sm', cardClass : 'instagram no-caption'}) | raw}}
                    {% endif %}

                </div><!--row-->
            </div>

        </div>
    </section>
